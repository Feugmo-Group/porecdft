# @package _global_.compute
# CUDA GPU, double precision — when accuracy matters more than memory.
# Requires JAX_ENABLE_X64=1 or compute.enable_jax_x64() in the run script.
use_warp:    true
warp_device: cuda:0
jax_device:  gpu
dtype:       float64
